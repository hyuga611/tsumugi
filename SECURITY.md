# SECURITY.md

## 何を守っているか

tsumugi はターミナルエミュレータなので、**画面に出た全部**（鍵・パスワード・
トークン）を握り、**シェルへ任意のキーを送れる**立場にある。ここでの
「破られた」は、たいてい「そのユーザとして任意のコマンドが走った」を意味する。

守っている境界は 1 本だけ、はっきりしている。

> **同じマシンの他のユーザから、セッションへ触れないこと。**

守っていないもの（そもそも守れないもの）も書いておく。

- **自分が走らせたプログラム**からは守らない。あなたが `cat` した内容も
  `curl | sh` したものも、あなたの権限で動く。端末はそれを止める場所ではない。
- **管理者**からは守らない。所有権を取る経路は OS の管理者の権能で、
  アプリが塞ぐ種類のものではない。
- **リモートは扱わない。** ソケットはローカルだけで、ネットワークを開かない。

## 信頼境界は mux ソケット 1 本

ウィンドウは常にクライアントで、状態を持つのは別プロセスの mux サーバ。
両者は JSON Lines で話す（`docs/rpc.md`）。**この口に繋げる者は、そのユーザ
として何でもできる。** したがってこの口の権限が、唯一にして全部の錠になる。

| | 置き場所 | 誰が触れるか |
|---|---|---|
| Unix | `$XDG_RUNTIME_DIR/tsumugi/`（無ければ `/tmp/tsumugi-<uid>/`） | 0700 のディレクトリ、ソケットは mode 0600 |
| Windows | `\\.\pipe\tsumugi-<印>-<名前>.sock` | 現在のユーザ SID だけを許す DACL（`D:P(A;;GA;;;<SID>)`） |

決めごと:

- **閉じられなければ開かない。** ソケットを自分だけに閉じられない環境では、
  サーバは起動に失敗する。開いた口を黙って使わせない。
- Unix は置き場所の**所有者と mode を開くたびに確かめる**。作った直後でも
  確かめるのは、作る前から在った場合があるため（`/tmp` は誰でも書ける）。
- Windows は繋いだ先のプロセスが自分のものかを**クライアント側でも確かめる**。
  `\\.\pipe\` は誰でも名前を作れる名前空間なので、DACL だけでは
  「他人が先に作った口へ自分が繋いでしまう」筋（squatting）が残る。
- ソケットの上に**追加の認証は置いていない**。ここへ繋げる時点で、その相手は
  すでに同じユーザとしてプロセスを起こせる。錠を二重にしても守るものが増えない。

## 端末として気をつけていること

子プロセスが出すバイト列は、**すべて敵の入力として扱う**。

- **OSC 52（クリップボード）を実装しない。** 実装すると、`cat` した 1 行で
  クリップボードを読み書きされる。持たないことが対策になる種類の機能。
- **OSC 0/2（タイトル）と OSC 7（cwd）を素通ししない。** 制御文字を落とし、
  長さを切る。素通しするとステータス行の見た目が壊れ、メモリが際限なく伸びる。
- **スクロールバックに上限がある**（既定 1 万行、`[scrollback] lines`）。
  上限が無いと、出しっぱなしのプロセス 1 つでメモリが尽きる。
- **保存は書いてから置き換える。** `:w` は同じディレクトリの一時ファイルへ
  書き、`fsync` してから `rename` する。途中で落ちても元の中身が空にならない。

## 依存

外部ネットワークを開くクレートは入れていない。`cargo tree` で確認できる。

## スクロールバックはディスクに残らない

mux サーバのメモリの中だけにある。**ファイルへ書き出す経路が無い。**
セッションの控えとして書くのはセッション名だけで、画面の中身は書かない
（`crates/tsg-mux/src/sessions.rs`）。端末のスクロールバックが一時ファイルへ
落ちる実装は過去に情報漏洩の元になっているので、構造的に持たない。

保存を頼まれたとき（`:w`）だけ、頼まれた場所へ書く。

## 報告

脆弱性を見つけたら、**公開の issue ではなく** GitHub の
[Security Advisories](https://github.com/hyuga611/tsumugi/security/advisories/new)
から報告してください。ここが唯一の非公開の窓口です。修正して公開できる形に
してから issue にします。

返事の目安: 受け取りの返信は 3 日以内、影響の見立ては 2 週間以内。

再現手順（できれば最小のもの）と、影響の見立てを添えてもらえると助かります。
「これは仕様の範囲では」と迷う場合も、まず送ってください。上の
「守っていないもの」に当たるなら、その旨を返します。

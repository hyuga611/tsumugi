# 変更の記録

形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/)、
版番号は [Semantic Versioning](https://semver.org/lang/ja/)。

## [0.3.4] - 2026-08-27

0.3.3 を会社の PC で実際に走らせて出た 2 つ。どちらも**当てにいっていた**のが原因。

### 直した

- **シェル統合が入らない PC があった。** `$PROFILE` の場所を
  `Documents\WindowsPowerShell` / `Documents\PowerShell` の**在るほうを探して**
  決めていたが、これは 2 通りに外れる — profile を 1 度も作っていない人は
  どちらの入れ物も無く「場所が分からないので自分で 1 行足してください」で
  終わる（会社の PC がこれ）。両方ある人には、使っていないほうを選ぶ。
  **当てにいかず、PowerShell 本人に `$PROFILE` を訊く。** 入れ物が無ければ作る。
  訊けない環境では、これまでどおり当てにいく。
- **`tsg update` が、powershell を起こせなかったときに tsg を失わせていた。**
  避けた `tsg.exe.old-…` を戻すのは「台本が失敗を返したとき」だけで、
  **起こせなかったとき**（`アクセスが拒否されました` など）はそのまま抜けていた。
  どの失敗の道でも戻す。戻したことと、手で入れ直す 1 行も出す。

## [0.3.3] - 2026-08-27

**入れるのを 1 行にした。** これまでは `irm | iex` のあとに
`--install-shell-integration` と `--install-agent-hooks` を自分で打つ必要があり、
打たなければ左のふちも `[[` も `ac` も `go` も効かない — **入れたのに動かない**
状態から始まることになっていた。入れ方を 3 つに割っていただけで、
打つ側にとっては 1 つの用事でしかない。

### 変えた

- **`tsg --install` が、シェル統合とエージェントのフックまで入れる。**
  今のシェルが分からなければ、その旨と打つべきコマンドを出して先へ進む。
  エージェントは入っているものだけに配線する（無いものは黙って飛ばす）。
  **何を変えたかは全部画面に出る。**
- **`tsg --uninstall` が、その 3 つとも戻す。** 入れる側が人の rc を書き換える
  以上、外す側で元へ戻らないなら書き換えてよい理由が無い。外すのは
  **こちらが足した行だけ**で、見出しの前に空けた 1 行まで持って帰る。

### 追加

- **`tsg --uninstall-shell-integration [シェル]`** — シェル統合だけを外す口。
  `--install-shell-integration` の片割れで、`--uninstall` もこれを通る。

### 直した

- README に制御文字が混ざっていた。`%USERPROFILE%\bin` の `\b` が
  **後退文字**に、`D:\tools` の `\t` が**タブ**になっていて、
  `%USERPROFILE%in` と読める状態で公開されていた。

## [0.3.2] - 2026-08-27

入れ替えたあとに残る手作業を消した。会社の PC で実際に詰まった 2 つ。

### 追加

- **`tsg update --stop-sessions`** — 入れ替えたあと、走っているセッションも
  止める。プロトコルが上がった版では、古い実行ファイルで動いているサーバに
  新しい exe から繋げないので、いつかは止めることになる。
  **既定では止めない** — 止めるというのは中のシェルとエージェントを
  終わらせることで、頼まれていないのにやっていい操作ではない。
- 既定でも、**走っているセッションの名前をそのまま出す**ようにした。
  「繋がらなくなったら `tsg -s <名前> --kill`」では、`<名前>` を自分で
  調べることになる。打てる形で出す。

### 直した

- **動いている tsumugi の上から入れられなかった。** `install.ps1` が
  「tsumugi is running. Close it first」と断っていたが、**閉じるというのは
  中のシェルを閉じること**で、作業中の人には払える代償ではない。動いている
  exe は上書きできないが**名前は変えられる**（Windows はファイルを追うので、
  開いている窓はそのまま動き続ける）ので、避けてから置くようにした。
  取ってくるのに失敗したときや起動確認に落ちたときは避けたものを戻す。
  前の入れ替えで残ったものは、次に入れるときに掃除する。

- **動いている tsumugi の上から入れられなかった。** 「tsumugi is running.
  Close it first」と断っていたが、**閉じるというのは中のシェルを閉じること**で、
  作業中の人には払える代償ではない。動いている exe は上書きできないが
  **名前は変えられる**（Windows はファイルを追うので、開いている窓はそのまま
  動き続ける）ので、避けてから置くようにした。取ってくるのに失敗したときは
  避けたものを戻す。前の入れ替えで残ったものは、次に入れるときに掃除する。
  入れ終わったら「開いている窓は古いままなので開き直すこと」と、
  古い多重化サーバの止め方を出す。

## [0.3.1] - 2026-08-27

0.3.0 で入れた `tsg update` が、**すでに最新のときに tsg を消していた**。
入れ替えのつもりで打った人が、次に `tsg` と打てなくなる。

### 直した

- **`tsg update` が、すでに最新のときに tsg.exe を失わせていた。**
  走っている自分は上書きできないので先に `tsg.exe.old-…` へ避けるが、
  台本は「もう最新です」と言って**何も置かずに、成功として**戻る。
  避けたままなので、置かれるはずだったものがどこにも無い状態が残った。
  置かれたかどうかを確かめ、置かれていなければ避けたものを戻すようにした。
  （「もう最新です」と言うだけの道が正しく、そこで名前が変わるのは
  こちらの都合でしかない。）
- Windows 以外でビルドが通らなかった。`use std::path::Path;` を非 Windows 側へ
  二重に入れていた。あわせて「`cargo build` の成果物には上書きしない」判定を
  共通の入口へ上げた（OS に依らない判断なので）。
- その判定の試験が Unix で素通りしていた。`C:\dev\…` と書くと Unix では
  まるごと 1 つのファイル名になる。パスは区切りごとの部品から組む。

## [0.3.0] - 2026-08-27

作業台。`cd` して **`go`** と打つと、いま居るペインを真ん中にして
**左にディレクトリの木・右に AI エージェント**が並び、**タブの名前が
そのディレクトリ**になります。プロトコルは 20 → 22（**古いサーバへは
繋がりません**。入れ替えたら `tsg -s <名前> --kill` で一度止めてください）。

### 追加

- **`tsg update`** — 最新版を取ってきて入れ替える。**入れ方は 1 つに保つ**ため、
  中で走らせるのは README と同じ `install.ps1`（しかも取ってくるのは常に
  最新の台本なので、古い exe でも新しい入れ方に従える）。いまの版を
  台本へ渡すので、**同じ版なら 19 MB を取り直さない**（`--force` で押し切る）。
  走っている自分は上書きできないので、先に `tsg.exe.old-…` へ避けて置き直し、
  **次の `tsg update` で消す**。`cargo build` で作った exe には上書きせず、
  断って代わりの手を出す（作者の機械ではそれが既定なので、黙って
  置き換わると次のビルドまで何が動いているのか分からなくなる）。
  裸の `update` も `--update` も受ける。
- **スクリーンショットを、AI へ出す指示のところへ貼れる。** `Ctrl＋Shift＋V`
  でクリップボードの絵を PNG にして置き、**そのパスをプロンプトへ打ち込む**
  （Enter は押さない）。画像をウィンドウへ落としても同じところへ着く。
  **新しいコマンドは足していない** — 「貼り付け」は 1 つの動詞で、
  入っているものに応じていちばん近いことをする（字なら字、絵ならパス）。
  書くのは**サーバ**で、置き場所はセッションごとの一時領域
  （遠隔（`[domains]`）で開いていれば、読む側は向こうに居る）。
  古いものから 32 枚だけ残す。
- **`go` で作業台を組む。** シェル統合が置く関数で、`tsg --workspace` を
  呼びます。キーからは `Space w`、パレットからは `:go`（`:go <パス>` で
  そこを開く）。割り付けは 20 / 50 / 30 で、**組むのは 1 通**
  （`ClientMsg::Workspace`）— 分割・木を開く・エージェントを起こす・
  タブに名前を付けるを別々に送ると、途中の形が配られて画面が跳ねるうえ、
  「いまできたペインの ID」を追いかける非同期の穴ができる。
  **Go 言語の `go` とは共存します**（引数があれば本物へ渡す。
  `TSUMUGI_NO_GO=1` で名前を渡さない）。
- **ディレクトリの木（左のペイン）。** `Buffer` を実装したので、`j` `k`
  `gg` `G` `/` も選択もマウスも**そのまま効きます** — 専用のモーションは
  1 行も書いていません（`concept.md` の「エディタ内蔵が実質タダになる」と
  同じ理屈）。木の中でだけ `Enter` 開く / `l` `h` 枝の開閉 / `a` `A`
  新しいファイル・フォルダ / `r` 名前を変える / `R` 読み直す。
  **既定のキーマップには入れていません**（端末で `a` や `r` の意味が
  変わるのは違う）。ダブルクリックで開き、**ドラッグ＆ドロップで動かせます**。
  `:q` で閉じれば、そのペインはふつうの端末に戻ります（下のシェルは
  走ったまま）。
- **木を読むのはサーバ側。** `[domains]` で遠隔に繋いでいるとき、
  ファイルが在るのは向こう。**開いてある枝の中だけ**を掘るので、
  `node_modules` や `target` の在る場所でも並べ直しが重くなりません。
- **タブに閉じるボタン（`✕`）。** `Space X` を知らない人が、タブを閉じる
  唯一の手段。未保存があれば 1 度止めます。
- **最小化していても届く知らせ。** タスクバーの点滅は、見ている人にしか
  気づけない。**どのタブが返事を待っているか**まで出します
  （`[ui] popup = false` で切れます）。窓が前に出たら片付けます。
- **`[workspace] agent`** — `go` の右で起こすもの。`"claude"` とも
  `["codex", "-m", "gpt"]` とも書けます。**既定は決め打ちしません**
  （`claude` を既定にすると、入れていない人の画面は
  「そんなコマンドはありません」で始まる）。

### 変えた

- 木の中では**書けません**。打鍵は下のシェルへも流しません
  （見えていないプロセスへ字が入るのが一番たちが悪い）。
- 木は末尾に貼り付きません（端末と違って、増えた先を追いかける相手ではない）。
  **自分が居た行を覚えている**ので、端末から戻っても同じ行から続けられます。

### 置かなかったもの

- **消す道。** 取り消せない操作を、指が滑る場所（一覧の上）へ置かない。
  消すのは真ん中のペインのシェルで。

## [0.2.1] - 2026-08-26

0.2.0 で塞がっていた 2 つの逃げ道。**版を上げた直後にサーバを止められない**、
**全画面アプリの上で画面の字を写せない**。どちらも「それしか手が無い」場面で
手が無くなる類で、機能が足りないのではなく**逃げ道が無い**のが問題だった。

### 追加

- **`tsg --kill`** — 走っているサーバを止める。**版が違っても効く**唯一の口で、
  `Attach` を通さずに `Shutdown` だけ送る。プロトコルを上げたあと、
  古いサーバが残っていると新しい実行ファイルからは何も言えなくなり、
  PID を手で探して殺すしか無かった（0.2.0 で実際に踏んだ）。
  版違いのエラー文にも、この直し方を書くようにした。
- **Shift＋ドラッグで、全画面アプリの上でも選べる。** AI の CLI のように
  マウスを要求するアプリを開いていると、なぞってもレポートが相手へ流れるだけで
  選択が残らず、**画面に出た字を写せなかった**。要求している以上「渡す」が
  既定として正しいので、渡さない合図を Shift に置いた（どの端末も同じ）。
  ステータス行の「🖱 アプリ側」にもこの逃げ道を書いた。
- **`Ctrl＋Shift＋C` でコピー**（`Ctrl＋Insert` でも）。中身は `y` と同じ道。
  全画面アプリを見ているあいだは打鍵が相手のものなので、
  貼り付けの `Ctrl＋Shift＋V` と対になるキーを置いた。

## [0.2.0] - 2026-08-26

外から足せる口と、編集の深さ。プロトコルは 12 → 20（**古いサーバへは
繋がりません**。入れ替えたら `tsg -s <名前> --kill` で、走っている mux を
一度止めてください）。

> 訂正: ここには当初 `--rpc` へ `{"t":"shutdown"}` を流すと書いていたが、
> **その道は版が違うと通らない**（`--rpc` は先に `Attach` を通すため）。
> 版に依らず止められる `--kill` を後から足した。

### 追加

- **`.` で直前の変更を繰り返す。** オペレータ × モーション × テキスト
  オブジェクトの全部の組に一律で効く。入力モードで打った字も含めて覚えるので、
  `ciwfoo<Esc>` の次の語で `.` を押せば同じ書き換えが起きる。`3.` は 3 回流す。
- **拡張の口**（`docs/rpc.md` §5）。別プロセスの拡張が
  ①出来事を購読でき（`command_end` / `pane` / `agent` / `cwd` / `command`）
  ②語彙を足せる（パレット・右クリックメニュー・`tsg --run` という**本体と
  同じ道**に載る）③`get_buffer` で中身を取り出せる。
  まず目で見るための `tsg --subscribe` も付けた。
  スクリプト言語を中で走らせないので、拡張が落ちても端末は落ちない。
- **構文木のテキストオブジェクト**（tree-sitter）。`if` / `af` が関数、
  `it` / `at` が型・構造体・クラス、`ia` / `aa` が引数ひとつ（`aa` はカンマごと。
  残すと消したのに構文が壊れる）。Rust / C / Python / JavaScript / Go / JSON。
  **端末では `af` は今までどおりパス** — 同じ字が、いま見ているものに応じて
  いちばん近いものを指す。
- **言語サーバの「直す側」**。`K` でその場の意味、`gr` で使われている場所
  （バッファに出るので `[[` も `af` もそのまま効く）、`gn` で名前を変える
  （取り消しは 1 段）。他のファイルにも書き換えが要るときは、**黙って一部だけ
  当てず**にその数を言う。
- **`config.lua`**。`config.toml` と同じ形の表を `return` すると、設定を
  計算して書ける（`tsumugi.os` / `tsumugi.hostname` / `tsumugi.env`）。
  同じ設定を機械ごとに分けるためのもので、拡張の場ではない
  （重いものは上の口の向こうへ置く）。両方あるときは `config.lua` が勝つ。
- **拡張が自分のペインを持てる**（`ext_pane_open` / `write` / `close`）。
  同じ id で開き直すと同じペインに書くので、押すたびに増えていかない。
  拡張が落ちたとき、**語彙は消えるがペインは残る**（押しても何も起きない
  項目は嘘になるが、読んでいる中身が消えるほうが困る）。
- **拡張が何をしたかの記録**（`tsg --ext-log`）。**断った理由が必ず出る。**
  サーバは「断る」と「記録する」を 1 か所にまとめてあるので、断り方が
  増えても書き忘れが起きない。`ext_hello` で名乗ると名前で出る。
- **条件を決めて待てる**（`tsg --wait --until exit:1 / text:… / re:…`）。
  `all` / `any` / `not` で組み合わせるときは生の口から
  （「終わって、しかも失敗した」は片方ずつ待つと間の一瞬を取りこぼす）。
  読めない正規表現は**待ち始める前に**断る。
- **並べ方を持ち運べる**（`tsg --layout-export` / `--layout-apply`）。
  書き出すのは形だけ（割る向き・取り分・葉で開くもの）で、ペインの番号は
  落とす。当てるのは**新しいタブ**——いまのタブを組み替えると、そこに
  居るペインを閉じることになる。
- **git の作業ツリー**（`tsg --worktrees` / `--worktree-add` / `--worktree-open`
  / `--worktree-remove`）。エージェントを並べるなら置き場所も要る。
  `--worktree-add` は足したあとそのまま新しいタブで開く。消すときは
  `--force` を渡さない（直しかけが残っていれば git が断る）。
- **タブ行とステータス行が窓と地続きになった。** 窓が透けているときは帯を
  塗らない。塗り重ねる限り、帯の透け方を窓と一致させる方法は「塗らない」
  しか無い（α 合成は必ず窓より濃くなる）。不透明な窓では今までどおり。
- **ステータス行に誤りの数とブランチ**（`✗3 ▲7  main +12 -3`）。診断は
  波線と同じ色。git は開いたときと保存したときだけ数え直す。
- **画面へ知らせられる**（`tsg --notify "終わりました" [--warn|--error]`）。
  台本や拡張から出す「終わったよ」。**溜めない** — 窓が開いていなければ
  どこにも出ない（後から出てくる知らせは、たいてい手遅れ）。
- **出来事でも待てる**（`tsg --wait --until event:pane`）。待ちと購読は
  同じ名前を使う。「起きるまで待つ」と「起きたら知らせて」は同じことを
  別の向きから見ているだけなので、名前の付け方まで分けない。
- **拡張の実例を置いた**（`examples/herdr-agents.py`）。別のターミナル
  多重化ランタイム（herdr）が抱えているエージェントを tsumugi の中で見る、
  という中身で、名乗り・語彙・購読・自分のペイン・知らせを全部通る。

### 修正

- 行末に空白がある行で `ciw` した直後、打った字が 1 つ手前へ入っていた
  （通常モードの丸め方を入力モードにも掛けていた）。

## [0.1.1] - 2026-08-26

### 追加

- **貼り付けを 3 通りに**（中クリック / Ctrl＋Shift＋V / Shift＋Insert）。
  読むモードの `p` は、名無しのレジスタが空なら**システムのクリップボード**から
  貼る（ブラウザでコピーした文がそのまま貼れる）。
- **ブラケットペースト**（`ESC[200~ … ESC[201~`）に対応。相手が `?2004h` で
  求めていれば囲って渡すので、**複数行を貼っても行ごとに実行されない**
  （Claude Code や vim が見ている）。囲いを求めていない相手へ複数行を貼った
  ときは、その旨を下の行に出す。
- 貼り付ける中身は**字だけに均す** — 改行は `CR` に揃え、制御文字は落とす。
  囲いの終わり（`ESC[201~`）を中身に混ぜて抜け出す手も塞がる。

## [0.1.0] - 2026-08-26

最初のリリース。ここまでのマイルストーンの詳細は `docs/m*-results.md` にあります。
Windows の exe（CRT 静的リンク）を Release に添付しているので、
`install.ps1` 1 行で入ります。macOS / Linux はビルドは通りますが**未検証**です。

### 追加

- **`Space X` でタブを閉じる**（中のペインごと）。`Space x`（ペイン 1 枚）とは別。
  最後のタブを閉じたら窓も仕舞う（セッションは生きているので開き直せば戻る）。
- **1 行で入れられるようにした**（`install.ps1`）。最新の Release から
  `tsg.exe` を取ってきて `tsg --install` まで済ませる。CRT を静的リンクした
  exe を配るので、VC++ 再頒布可能パッケージが無い PC でも動く。

- **Shift＋Enter で改行**（素の Enter は送信）。`ESC CR` を送る。AI エージェント
  の入力欄で「送信せずに改行」ができる。

### 修正

- **右端の折り返しを「次の 1 字」まで保留するようにした。** 保留を桁で
  表していた（`col == cols`）ので、そのあとに来る改行やカーソル移動でも
  保留が残り、次に字が出たときにもう 1 行送っていた。**打った字が 1 行下の
  別の桁に出る・前の出力に字がかぶる**という形で出る（ConPTY は絶対座標で
  位置を指すので、ずれた瞬間に重なる）。
- **面で出来ている字（`▛▜▝█` U+2580..U+259F）を自分で塗るようにした。**
  Consolas にも MS Gothic にも四分割の字が無く、Claude Code のロゴが
  崩れていた。割合で塗るので、どの字体でも隙間なく敷き詰まる。
- **記号の段をフォールバックに足した**（Windows は Segoe UI Symbol）。
  `❯`（入力欄の印）・`⏵`・`⠋`（スピナー）がどのフォントにも無く、
  何も描かれていなかった。
- **セル幅からはみ出す字形を縮めて入れるようにした。** フォールバックの
  フォントは等幅ではないので、1 セルの記号（`✳` `✔` `⚠`）が 1.5〜2 セルの
  幅で来て隣の字に重なっていた。

全画面アプリ（Claude Code・vim など）を起動 → Ctrl-C で抜ける → 起動しなおす、
で画面が崩れていた。

- `CSI > 4 m`（修飾キーの設定。全画面アプリが終わるときに出す）を SGR 4 と
  読んでいて、**下線が点きっぱなし**になっていた。`CSI < u` も同じ形で、
  カーソルの復元と取り違えていた。中間バイトの付いた CSI は別物として扱う。
- 再アタッチの控え（`snapshot`）が履歴と alt screen を 1 本の文書に混ぜていた。
  受けた側は alt に居ることを知らないので、アプリの絵が履歴に焼き付き、
  アプリが終わっても消えなかった。Esc とマウスの行き先も間違えていた。
  → プロトコル **版 12**（`alt` / `alt_cursor` を追加）。
- `ESC[3J`（履歴を捨てる）が印（OSC 133）と絵を寄せていなかった。
- 保存カーソル（DECSC）とスクロール領域が primary と alt で共有だった。
- ソフトリセット（`CSI ! p`）と `CSI s` / `CSI u` を受け取っていなかった。
- RIS（`ESC c`）がマウスレポートなどのモードを戻していなかった。
